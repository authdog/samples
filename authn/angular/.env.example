# Public key (pk_...) from console → environment settings.
# Never put the secret key (sk_...) here.
# Angular has no built-in .env; set it via your shell or a define plugin.
AUTHDOG_PUBLIC_KEY=pk_...
