<main>
  <h1>authdog observability (SvelteKit)</h1>
  <p>
    This starter consumes the identity event stream. It does not run
    detectors — that is Lidar, in the console.
  </p>
  <ul>
    <li><code>POST /webhooks/authdog</code> — signed webhook receiver</li>
    <li><code>GET /events</code> — first page of the Events API plus the cursor</li>
  </ul>
</main>
