# Server-only secrets. Never expose to a browser or commit real values.
# Astro: no PUBLIC_ prefix keeps them server-only.
AUTHDOG_API_TOKEN=
AUTHDOG_WEBHOOK_SECRET=
AUTHDOG_TENANT_ID=
AUTHDOG_ENVIRONMENT_ID=
