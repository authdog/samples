---
---

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>authdog observability — Astro</title>
    <style>
      body { font-family: system-ui, sans-serif; margin: 2rem auto; max-width: 40rem; line-height: 1.5; }
      a, button { font: inherit; }
    </style>
  </head>
  <body>
    <main>
      <h1>authdog observability (Astro)</h1>
      <p>
        This starter consumes the identity event stream. It does not run
        detectors — that is Lidar, in the console.
      </p>
      <ul>
        <li><code>POST /api/webhooks/authdog</code> — signed webhook receiver</li>
        <li><code>GET /api/events</code> — first page of the Events API plus the cursor</li>
      </ul>
    </main>
  </body>
</html>
